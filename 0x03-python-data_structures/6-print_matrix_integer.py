#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for sub_matrix in matrix:
        if len(i) == 0:
            print()
        for num in range(sub_matrix) - 1:
            print(
                    "{:d}".format(sub_matrix[i]),
                    end="\n" if i is len(submatrix) - 1 else " ")
